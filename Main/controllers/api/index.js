const router = require('express').Router();
const userRoutes = require('./userRoutes');
const characterRoutes =require('./characterRoutes');

router.use(/* insert route to use */);
router.use(/* insert route to use */);

module.exports = router;